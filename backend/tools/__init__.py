"""Repository tooling that is neither application code nor a test (T-607).

Sibling of ``evals/``: importable from the backend root, excluded from the wheel
(``pyproject.toml`` ships ``app`` and ``workers`` only).
"""
